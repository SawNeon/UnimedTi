package com.unimedvargina.UnimedVarginhaTi.modules.printers.model;

/** De onde veio o numero da leitura. */
public enum ReadingSource {
    /** Importado do relatorio do PrintWay. */
    PRINTWAY,
    /** Digitado, para a impressora que o PrintWay nao le. */
    MANUAL
}
